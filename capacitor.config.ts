import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Canarias-Oculta-PWM-Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
